/**
 * <b>Data access.</b> Spring Data JPA repositories, plus any hand-written SQL such as the
 * conditional stock updates (ADR-008).
 *
 * <p>Stage 1 is organised by technical layer on purpose (constitution Article I). Reorganising
 * these packages by business area is Stage 2.
 */
package com.glendas.shopper.repository;
