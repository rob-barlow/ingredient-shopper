/**
 * <b>Business rules.</b> The clamp rule, order placement, stock adjustments, recipe proposals.
 * Called by controllers. Uses repositories and clients. Owns transactions.
 *
 * <p>Stage 1 is organised by technical layer on purpose (constitution Article I). Reorganising
 * these packages by business area is Stage 2.
 */
package com.glendas.shopper.service;
