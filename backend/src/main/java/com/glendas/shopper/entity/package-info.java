/**
 * <b>Persistence model.</b> JPA entities mapped to the Flyway-managed tables (plan §4).
 * Not exposed over HTTP. Controllers map them to the generated API models.
 *
 * <p>Stage 1 is organised by technical layer on purpose (constitution Article I). Reorganising
 * these packages by business area is Stage 2.
 */
package com.glendas.shopper.entity;
